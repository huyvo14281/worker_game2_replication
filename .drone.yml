kind: pipeline
name: default

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker container ls
  - ls
  - pwd

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock